
# 3ajjel  dob el radem bukra byekhla2ouna wledna w bishufo 4 seconds after that- here when she want to get on his back

# resets and shakes twice

set_frps 200
sync {
    move(1, 0)
    move(2, 0)
    move(4, 0)
    move(3,0)

}

# HERE
# Shake up/down with crescendo at 4 FRPS - varied movement

repeat 3
{
set_frps 4.0

# Crescendo shakes — increasing amplitude with asymmetric movement
sync { move(1, 0.65) move(2, 0.63) move(4, 0.64) }   # baseline - raised

# Small shake - alternating leads
sync { move(1, 0.68) move(2, 0.60) move(4, 0.66) }
sync { move(1, 0.62) move(2, 0.66) move(4, 0.63) }
sync { move(1, 0.66) move(2, 0.62) move(4, 0.68) }

# Medium shake - more varied rhythm
sync { move(1, 0.70) move(2, 0.58) move(4, 0.67) }
sync { move(1, 0.60) move(2, 0.68) move(4, 0.61) }
sync { move(1, 0.68) move(2, 0.60) move(4, 0.69) }
sync { move(1, 0.62) move(2, 0.66) move(4, 0.60) }

# Large shake (crescendo peak) - complex patterns
sync { move(1, 0.72) move(2, 0.56) move(4, 0.70) }
sync { move(1, 0.58) move(2, 0.70) move(4, 0.59) }
sync { move(1, 0.70) move(2, 0.58) move(4, 0.71) }
sync { move(1, 0.60) move(2, 0.68) move(4, 0.58) }
sync { move(1, 0.69) move(2, 0.59) move(4, 0.69) }

wait(0.5)

# Settle center (higher position)
set_frps 0.06
sync {
    move(1, 0.65)
    move(2, 0.63)
    move(4, 0.64)
}
}


# knock



# HERE
# Shake up/down with crescendo at 4 FRPS

repeat 3
{
set_frps 4.0

# Crescendo shakes — increasing amplitude
sync { move(1, 0.50) move(2, 0.48) move(4, 0.49) }   # baseline

# Small shake
sync { move(1, 0.52) move(2, 0.46) move(4, 0.51) }
sync { move(1, 0.48) move(2, 0.50) move(4, 0.47) }

# Medium shake
sync { move(1, 0.54) move(2, 0.44) move(4, 0.53) }
sync { move(1, 0.46) move(2, 0.52) move(4, 0.45) }

# Large shake (crescendo peak)
sync { move(1, 0.56) move(2, 0.42) move(4, 0.55) }
sync { move(1, 0.44) move(2, 0.54) move(4, 0.43) }

wait(0.5)

# Settle center
set_frps 0.5
sync {
    move(1, 0.5)
    move(2, 0.48)
    move(4, 0.49)
}
}
