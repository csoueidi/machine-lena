# when they become in machine behind -> al 3omro alathi here
 
 
sync{ 
move(1, 0.7,0.035)

move(2, 0.55, 0.02)

move(4, 0.15, 0.02)
}
 
wait(3)

 
set_frps 0.05
sync {
move(1,0)

move(2,0)

move(4,0)
}

wait(1.5)

set_frps 10000
move(1,0.15)
move(1,0)

 