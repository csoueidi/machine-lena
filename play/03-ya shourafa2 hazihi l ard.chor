# When Ziad sings, al zar3ou fawkaha lana wal nift - here
# 39 seconds

# Gentle sway right
set_frps 0.02
sync {
    move(1, 0.3)
    move(3, 0.2)
    move(4, 0.3)
    move(2, 0.28)
}


# Sway left
set_frps 0.03
sync {
    move(1, 0.4)
    move(3, 0.45)
    move(4, 0.4)
    move(2, 0.38)
}

wait(2)

# Reset to zero for next scene
set_frps 0.04
sync {
    move(1, 0)
    move(3, 0)
    move(4, 0)
    move(2, 0)
}
