# bteer ma3 naweris la estad l samak - Then Ziad whistles - after first whistle

set_frps 0.02
sync {
  move(1, 0.15)
  move(2, 0.15)
  move(4, 0.1)
}

sync {
  move(1, 0.45, 0.4)
}

wait(0.5)

sync {
move(1, 0)
move(2, 0)
}