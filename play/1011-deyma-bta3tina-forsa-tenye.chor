  
    repeat 9{
        sync {  
                move(all, 0.1,1000);
                move(all, 0,1000);
            }
    }
    repeat 9{
        sync {  
                move(all, 0.1,1000);
                move(all, 0,1000);
            }
    }